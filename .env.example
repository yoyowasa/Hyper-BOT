# Network selection
# mainnet|testnet (default: testnet for safety)
HL_NETWORK=testnet

# Optional explicit endpoints (override network defaults if set)
# HL_BASE_URL=https://api.hyperliquid-testnet.xyz
# HL_WS_URL=wss://api.hyperliquid-testnet.xyz/ws

# Wallet (DO NOT COMMIT REAL KEYS)
# Provide per-process only when placing real orders
# HL_PRIVATE_KEY=0x...
# HL_ADDRESS=0x...

# Runtime
LOG_LEVEL=INFO

# Data directory for snapshots
# DATA_DIR=data

